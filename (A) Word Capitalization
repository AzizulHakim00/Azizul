#include<iostream>
using namespace std;
int main(){
    string word;
    cin>>word;
    int n = word.size();
    if(word[0] >= 65 and word[0] <= 90 ){
        word[0] = word[0];
    }
    else word[0] = word[0] - 'a' + 'A';
    cout<<word;
return 0;
}
